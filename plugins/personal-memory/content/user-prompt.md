### Memco Personal Memory

If you have not already done so this session, call `start_session` on Memco Personal Memory to
load my identity and preferences — do this before answering.

Apply what it returns to this reply: response style, formatting, tools, and workflow habits.

If this message tells you something durable about how I want you to work, search Memco Personal
Memory first, then `write_memory` it — one preference per memory, updating an existing note in
place rather than writing a near-duplicate. Anything a teammate could reuse belongs in shared
memory instead, not here.
